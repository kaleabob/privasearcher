version: 1
services:
  - name: privasearcher
    type: web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:10000 --workers 2
    envVars:
      - key: PORT
        value: 10000
